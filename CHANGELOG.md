# Changelog — 온라인 심사 시스템 (judge.sw4u.kr)

버전 체계: `v{YY}.{M}.{D}` (배포일 기준). 버전 기록은 이 파일에만 남긴다.
(웹 화면의 우측 상단 `?` 버튼은 v26.7.15부터 사용 설명서 — `resources/views/partials/manual.blade.php`.
기능이 바뀌면 설명서의 해당 섹션도 함께 갱신할 것.)

## v26.9.5 (2026-09-04) — 안드로이드 앱(APK) · 모바일 심사 화면 전면 개편

### 개선 — 심사 화면 모바일/태블릿 최적화
- **대상 목록을 좌측 드로어(햄버거)로 이동.** 기존에는 lg 미만에서 대상 목록이 점수 입력 폼 **위에**
  통째로 쌓여, 대상이 100명이면 약 4,400px를 스크롤해야 첫 점수 칸에 닿았다. 이제 첫 화면에 점수 칸이 바로 나온다
- 드로어에 **검색**(심사번호·이름)과 **전체/미완료/완료 필터** 추가 — 대상이 많은 행사에서 찾기
- **상단 고정 바** — 현재 대상 · 위치(n/전체) · **합계**(스크롤해도 안 사라짐) · 진행 막대
- **하단 고정 바** — `[← 이전]` `[저장하고 다음 →]`. 엄지 닿는 위치, safe-area 대응
- **[저장하고 다음]** — 저장과 이동을 한 번에. 미완료 필터를 켜면 자동으로 미완료만 순회한다.
  저장이 거절되면(마감·검증 실패) 이동하지 않는다. 마지막 대상에서는 [저장하고 마치기] → 서명 안내
- **점수 스테퍼** — 큰 `−` / `+` 버튼(1탭 1점, 길게 누르면 가속). 가운데 숫자를 탭하면 0.5점 단위 직접 입력.
  0 ~ 배점 범위로 자동 제한
- 데스크톱(lg 이상) 2열 레이아웃은 그대로 유지

### 기능 추가 — 안드로이드 앱
- Flutter WebView 래퍼 앱 신규 (`/var/services/web/apps/judge-app`, applicationId `kr.sw4u.judge_app`).
  플레이스토어를 거치지 않고 사이트에서 APK 를 직접 배포한다
- **`/app` 내려받기 페이지** — 버전·용량·공개일·SHA-256, QR 코드, "출처를 알 수 없는 앱" 설치 안내,
  아이폰 사용자용 홈 화면 추가 안내. 홈 화면과 사용 설명서에 링크 추가
- 앱 안에서는 "앱 설치"·인쇄 버튼을 감춘다 (User-Agent 의 `JudgeApp/<버전>` 로 판별)
- 오프라인 채점은 앱 안에서도 웹과 동일하게 동작한다 (서비스워커 + localStorage 재사용)
- 릴리스 서명 안전장치 — 등록된 키가 없으면 릴리스 빌드가 중단되고, 게시 전 인증서 지문을 대조한다

### 수정
- nginx: `sw.js` 와 `downloads/judge-latest.apk` 에 `Cache-Control: no-cache`.
  Cloudflare 엣지가 정적 파일을 4시간 캐시해, 이름이 그대로인 채 내용만 바뀌는 이 둘이
  옛 버전으로 응답하던 문제 (서비스워커는 `SW_VERSION` 을 올려도 몇 시간 늦게 도착했다)
- nginx: APK 를 `application/vnd.android.package-archive` 로 서빙 (`.apk` 확장자에만 적용).
  server 블록에 `types {}` 를 쓰면 상속된 MIME 맵이 통째로 덮여 `sw.js` 까지 octet-stream 이 되므로 쓰지 않는다

## v26.9.4 (2026-09-04) — 앱(PWA) 전환 · 오프라인 채점

### 기능 추가 — 설치형 앱(PWA)
- 홈 화면에 설치해 **주소창 없는 전체화면 앱**으로 실행 (`public/manifest.json`, `public/sw.js`)
- 앱 아이콘 6종 — neis.me Toolgrid 마크에 judge 강조색(indigo). 생성기 `tools/make-icons.php`
- 헤더 우측에 **앱 설치 버튼**(`partials/pwa-install.blade.php`). 안드로이드는 바로 설치,
  iOS 사파리는 "공유 → 홈 화면에 추가" 안내 팝오버
- 오프라인 안내 화면 `public/offline.html` (CSS 인라인 — CDN 없이도 온전히 렌더)
- Tailwind·Alpine·Pretendard CDN을 서비스워커가 캐시 — 끊겨도 화면이 깨지지 않음

### 기능 추가 — 심사위원 오프라인 채점
- 입력값을 제출 전에도 기기에 보관 → **대상을 바꾸거나 앱을 닫아도 입력이 사라지지 않음**
  (기존에는 저장 전 입력이 대상 전환만으로 유실됐다)
- 끊긴 상태로 [점수 제출]을 눌러도 기기 대기열에 담기고, 연결 복귀 시 자동 전송.
  대상 목록에 **대기** 배지, 상단에 연결 상태 안내
- 전자서명도 동일하게 대기열 처리 (기존에는 실패 시 `alert`만 뜨고 사라졌다)
- 재전송 전 `GET /csrf`로 토큰 재발급 — 캐시된 화면의 만료 토큰(419) 대응
- 심사 마감(423)·검증 실패(422)는 대기열에서 제거하고 사유를 안내 (무한 재시도 방지)
- 저장 실패 시 `"Failed to fetch"` 원문 노출 → 한국어 안내 문구로 교체

### 변경
- 관리자 화면은 **의도적으로 캐시하지 않음** — 오래된 집계를 최신으로 오인할 위험 차단
- 라우트 추가: `GET /csrf` (`csrf.token`) — PWA 오프라인 큐 전용 토큰 재발급
- 사용 설명서: 심사위원 가이드에 "앱으로 설치하기"·"인터넷이 끊겨도 괜찮습니다" 추가, FAQ 2문항 추가
- README에 "PWA — 앱으로 설치해 쓰기" 섹션 추가

## v26.7.15 (2026-07-15) — 사용 설명서 추가 · 홈 화면 분리 · 명칭 통일

### 변경
- 헤더 우측 `?` 버튼: 버전 업데이트 기록 모달 → 초보자용 사용 설명서 모달로 교체
  (탭 구성: 처음이신가요? / 관리자 가이드 / 심사위원 가이드 / 자주 묻는 질문)
- 화면용 버전 기록(`partials/changelog.blade.php`) 제거 — 버전 이력은 이 CHANGELOG.md에만 유지
- 홈 화면을 심사위원 입장 전용으로 분리 — 행사 생성 폼과 목록 버튼 제거, 하단에 "행사 만들기 · 관리" 링크만 배치
- `/events`를 "행사 관리" 페이지로 개편 — [＋ 새 행사 만들기] 토글 폼(홈에서 이동) + 행사 목록. 행사가 없거나 입력 오류 시 폼 자동 펼침
- 관리자 UI 명칭 통일: "프로젝트" → "행사" (홈·목록·생성 메시지·매뉴얼 전체)

### 기능 추가 — 블라인드 심사 (심사번호)
- 평가 대상에 등록순 2자리 **심사번호**(01, 02…) 자동 부여 (`Event::candidateNumbers()`)
- 심사위원 평가 화면: 기관/이름 대신 심사번호만 표시 — 이름·소속은 payload에도 싣지 않음, 저장 완료 메시지도 번호로
- 개별심사표(심사위원 인쇄 + 관리자 대시보드 출력): 연번·평가 대상(이름) 열 → 심사번호 열로 교체
- 최종집계표: 연번 열 → 심사번호 열, 기관/이름과 함께 표기 + 블라인드 안내 ※ 문구
- 관리자 대시보드·평가대상 목록에 심사번호 배지 표시, CSV에 심사번호 열 추가
- 매뉴얼·평가대상 화면에 블라인드 방식과 "중간 삭제 시 번호 당겨짐" 주의 안내

### 개선 — 행사 관리 화면 레이아웃
- 관리 탭 메뉴(기본설정·평가대상·심사위원·집계)를 화면 최상단으로 이동 + 스크롤 시 상단 고정(sticky)
- [심사 마감하기/재개하기] 버튼을 기본설정 탭으로 이동 — 행사명 우측에 상태 배지(심사 진행중/마감됨)와 나란히 배치
- 행사명 아래 행사일 표기 제거, 기본설정 집계 방식 섹션의 ※ 안내 문구 2줄 제거 (집계방식·합격자 수 입력만 간결하게)
- 집계 방식 라벨 축약: "평가대상별 최고·최저 총점 심사위원 제외" → "최고·최저 점수 제외"
- 사이트 이름 변경: "실시간 심사 평가" → **"온라인 심사 시스템"** (헤더 로고·title·푸터·README·이 파일 제목). 홈 첫 화면 제목도 "온라인 심사 시스템" + 회색 안내 문구(부여받은 심사위원 코드를 입력하거나, 전달받은 링크로 바로 접속하세요)

## v26.7.9 (2026-07-09) — 최초 릴리스 및 당일 개선

### 최초 릴리스
- 심사위원: 고유 코드/링크 접속, 대상별 항목 점수 입력 (100점 만점, AJAX 무새로고침)
- 심사위원: 제출한 점수 확인·수정(재제출), 전자서명(서명패드), 개인 심사표 인쇄
- 관리자: 프로젝트(행사) 생성 — 회원가입 없이 비밀번호만으로 관리
- 관리자: 평가 대상 / 평가 항목(배점 합계 100 강제) / 심사위원 일괄 등록
- 관리자: 실시간 집계 대시보드(5초 자동 갱신) — 총점·평균·순위·심사위원별 점수·진행률
- 관리자: CSV 다운로드(Excel 한글 호환), 최종결과 A4 1장 출력(담당자/확인자 결재란)
- 스택: Laravel 13 + MariaDB + Blade/Tailwind/Alpine, vm1 docker 스택(NPM→nginx→phpfpm) 서빙

### 기능 추가
- 행사 삭제 — 행사명 재입력 확인 후 관련 데이터 전체 삭제 (FK cascade)
- 프로젝트 목록(게시판형) — 목록 클릭 시 해당 프로젝트 설정으로 이동 (비밀번호 인증)
- 심사위원 평가화면 상단 진행중/마감 상태 배지
- 버전 업데이트 기록 `?` 버튼 (헤더 우측)
- 집계 방식 설정 — 전체 합계·평균(기본) / 평가대상별 최고·최저 총점 심사위원 제외(채점 3인 이상 시 적용). 실시간 집계·CSV·최종집계표에 공통 적용, 대시보드·최종집계표에 ※ 안내 문구 자동 표기

### 개선
- 버튼 재배치 — 설정 화면: [심사 실시간집계][심사 마감하기] / 대시보드: [설정][CSV 다운로드][최종결과 출력], 헤더에는 로그아웃만
- 인쇄물 좌우 여백 확대 (심사표 15→18mm, 최종결과 12→18mm + 본문 6mm)
- 심사위원 접속 코드를 6자리 숫자로 변경 (입력 편의)
- 심사 마감 시 접속 코드 자동 회수 → 코드 중복 원천 차단, 재개 시 새 코드 발급
- 인프라: DB.ini 중앙 접속설정 연동 (db-primary), Redis 캐시 적용 (redis-primary)
- 최종집계표 개편 — 제목 "{프로젝트명} 최종집계표", 연번(등록순) 정렬, 심사위원별 점수·총점·평균·순위 열 구성
- 개별심사표 개편 — 제목 "{프로젝트명} 개별심사표", 제목 아래 우측 정렬 "심사위원: {이름} (서명)" 밑줄 서명란(전자서명 삽입 지원), 그 아래 점수표
- 대시보드에 집계방식 배지 표시, 최종집계표 평균 소수점 2자리 고정 (25 → 25.00)
- 최종집계표: 평가대상별 최고·최저 총점으로 제외된 심사위원 점수를 붉은 취소선으로 표시, ※ 범례 자동 추가 (제외 방식일 때만)
- 심사위원 3명 미만 + 제외 방식이면 설정 화면(배너)·대시보드(배지)에 "제외 없이 전체 집계" 경고 표시
- 대시보드에도 제외된 점수를 붉은 취소선으로 표시 (실시간 반영, ※ 범례 포함)
- 평가항목 2단계 구조 — 대분류(A, B, C…) 아래 서브항목(b1, b2…) 등록 가능. 채점은 말단 항목(서브항목 또는 서브 없는 대분류)에서만. 대분류 합계=100, 서브 합계=대분류 배점 검증(불일치 경고 배너). 평가화면 그룹 헤더+소계, 개별심사표 2단 헤더(colspan). 점수가 이미 입력된 항목에는 서브항목 추가 불가
- 대시보드 심사위원 카드에 🖨️ 개별심사표 출력 버튼 (관리자 세션으로 접근, 코드 회수 후에도 출력 가능)
- 심사위원 접속안내 출력 페이지(`/admin/{event}/judges/print`) — 심사위원별 QR코드(스캔 시 바로 접속) + https://judge.sw4u.kr/ 주소 + 6자리 코드, 절취선 카드 2열 (설정 화면 "📱 접속안내 출력" 버튼)
- 최종집계표 하단 결재란 변경 — 담당자/확인자 → 심사위원 전원 서명란 (2열, 밑줄 안 연한 (서명) 라벨, 저장된 전자서명 자동 삽입)

### 버그 수정
- 심사위원 링크 복사 실패 — 비보안 컨텍스트에서 Clipboard API 부재 시 execCommand 폴백 + 수동 복사 안내
- 코드 회수(null) 상태에서 설정 화면 500 오류 — 심사위원 라우트 바인딩을 코드/id로 분리
- HTTPS 접속 시 "안전하지 않은 폼" 경고 — 내부 nginx realip 구조에 맞춰 `trustProxies('*')` 적용
